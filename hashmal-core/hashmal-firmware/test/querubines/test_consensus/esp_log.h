/*
 * esp_log.h - test stub.
 * The real ESP-IDF esp_log.h is not on the include path in the host
 * build. This stub is here so `#include "esp_log.h"` inside the
 * firmware sources resolves; the actual mock content is in
 * mock_esp_log.h and has already been pulled in by the test.
 */
#ifndef ESP_LOG_H_STUB
#define ESP_LOG_H_STUB
#include "mock_esp_log.h"
#endif
